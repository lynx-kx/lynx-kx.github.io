---
title: C++ basic 
tags:
    - basis
---

C++语言特性：编译、静态类型、多态。同时包含低层级的效率和高层级的类结构
https://web.stanford.edu/class/cs106l/full_course_reader.pdf
## stream 输入输出流
- 命令行console    cin cout
- 文件读写 fstream

```c++
ifstream myStream("myFile.txt");
myStream >> myInteger;

ifstream myStream;
myStream.open("myFile.txt");
```
some manipulator
```c++
cout << boolalpha << true << endl;// true
cout << setw(5) << 10 << endl; // width
cout << setprecision(5) << 10 << endl; // precision
cout << fixed << setprecision(5) << 10 << endl; // .precision
cout << setfill('-') << setw(COLUMN_WIDTH) << "" << "-+-";// fill
cout << setw(COLUMN_WIDTH) << "" << setfill(' ') << endl;
cin >> ws >> value;// skip whitespace
cout << dec << 10 << endl;// oct8 dec10 hex16
```
some readIn example
```c++
while(input >> intValue >> doubleValue) {   }

while(cin){// until EOF
    cin.get()// one char
}
char a[20];
cin.get(a,20) // 20 char

stringstream ss;// string stream
string myStr;
getline(cin,myStr);
ss << myStr;
ss >> a >> b;
```
Random Access(to be continued)

## 编译过程
- preprocessing 
- compile, to object code
  - Maybe compile error : syntax error
- linking, into final executable
  - Maybe link error : prototyped in one file but implement in another.


preprocessing
- #include directives
- $__FILE__ __line__$
- #define macros
  - pay attention to operator precedence
  - better use const not define
```c++
#if 
#elif
#else
#endif

#ifndef
#ifdef
#endif

#define PLUS_ONE(x) ((x) + 1)
#define MAX(a, b) ((a) > (b) ? (a) : (b))
inline int Max(int one, int two) {// suggestion to compiler
    return one > two ? one : two;
}
cout << __DATE__ << " " << __TIME__ <<endl;
cout << __FILE__ << " " << __LINE__ <<endl;
#define PRINTOUT(n) cout << #n << " has value " << (n) << endl    // #n is a string
PRINTOUT(x*3);
```
assert: in debug but not in release. no runtime overhead
## STL
Standard Template Library. 
- container
- Iterator
- algorithm
- adapter(stack、istream_iterator)
- functor
- allocator
### container
pair
```c++
pair<int,string> myPair;
myPair.first=137;
myPair.second="hello";
myPair = make_pair(137,"hello");
```
vector(contiguous memory)
```c++
size_t // non-negative
vector<int> myVector(NUM_INTS);
int n = myVector.size()
//generate(myVector.begin(), myVector.end(), rand);
myVector.push_back(300);// pop_back
//sort(myVector.begin(), myVector.end());
myVector.insert(myVector.begin()+1,500);// erase
myVector.erase(myVector.begin()+1,myVector.end()-1);// [a,b)
myVector.resize(5);// resize if more fill with 0
//copy(myVector.begin(), myVector.end(), ostream_iterator<int>(cout, "\n"));
myVector.clear();// size to 0
myVector.front();// T
myVector.back();// T
```
deque:double-ended queue(lists of pages)
- access slow
- insert fast
- choice when most insert and delete at beginning or end.
```c++
deque<int> dq;
dq.push_front();
dq.pop_front();
```

set:
check if the number exists.(birthday paradox)
```c++
set<int> s;
set<int> mySet(myVec.begin(),myVec.end());
s.empty();//  bool
s.insert(100);// [begin,end)
s.find(100)!=s.end();// iterator
s.count(100);// 1 or 0
s.erase(100);
```
map: representing relationship
```c++
map<string,int> myMap;
myMap["STL"]=137;

pair<map<string, int>::iterator, bool> result = myMap.insert(make_pair("STL", 137));
if(!result.second)
    result.first->second = 137;
```
multimap and multiset: not unique
```c++
pair<multimap<string, int>::iterator, multimap<string, int>::iterator>
        myPair = myMultiMap.equal_range("STL");
/* Iterate over it! */
for(multimap<string, int>::iterator itr = myPair.first; itr != myPair.second; ++itr)
cout << itr->first << ": " << itr->second << endl;
```
application: DFA and NFA
### adapter
stack and queue
```c++

```
valarray(to be continued)

### iterator
```c++
// advancing the iterator
for(vector<int>::iterator it = myVector.begin();it != myVector.end(); ++it){ // [begin,end)
    cout << *it << endl; // dereference
}
for(vector<string>::iterator it = myVector.begin();it != myVector.end(); ++it){ // [begin,end)
    cout << (*it).length() << it->length() << endl; // dereference
}
set<int>::iterator stop = mySet.upper_bound(100);// first > 100
for(set<int>::iterator itr = mySet.lower_bound(10); itr != stop; ++itr) {  }// first >=10

ifstream input("my-file.txt");//stream iterator
copy(istreambuf_iterator<char>(input), istreambuf_iterator<char>(), ostreambuf_iterator<char>(cout));
```

### STL algorithm
for simplicity, correctness, speed, clarity
```c++
accumulate(values.begin(), values.end(), 0.0);
count(myVec.begin(), myVec.end(), 137);
bool IsEven(int value) {
    return value % 2 == 0;
}
count_if(myVec.begin(), myVec.end(), IsEven);
fill(myDeque.begin(), myDeque.end(), 0);
generate(myVector.begin(), myVector.end(), rand);
// reorder
bool ComparePlaces(placeT one, placeT two) {
    if(one.x != two.x)
    return one.x < two.x;
    return one.y < two.y;
}
sort(myVector.begin(), myVector.end(),ComparePlaces);// for random access iterator
sort(myVector.begin(), myVector.end(),greater<int>());// for random access iterator
random_shuffle(myVector.begin(), myVector.end());
rotate(v.begin(), v.begin() + 2, v.end());
// search
if(find(myVector.begin(), myVector.end(), 137) != myVector.end())
if (binary_search(myVector.begin(), myVector.end(), 137)) { }// for sorted
// iterator adaptor
copy(myVector.begin(), myVector.end(), ostream_iterator<int>(cout, "\n"));// output
back_insert_iterator<vector<int>> itr(myVector);// like push_back
reverse_copy(original.begin(), original.end(), back_insert_iterator< vector<int> >(destination));
set_union(  setOne.begin(), setOne.end(), // All of the elements in setOne
            setTwo.begin(), setTwo.end(), // All of the elements in setTwo
            inserter(result, result.begin()));
copy(istream_iterator<int>(input), istream_iterator<int>(), inserter(values, values.begin()));// cin
myVector.erase(remove(myVector.begin(), myVector.end(), 137), myVector.end());
transform(s.begin(), s.end(), s.begin(), ::toupper);
```
Boost algorithm(to be continued)

## 面向对象-类
- abstraction
  - interface
    - attribute
    - action
  - constructor and destructor
    - default nullary constructor if not exists
  - template
    - need to put class and definition inside the header file.
    - define iterator needs typename: typename deque<T>::iterator
- principle
  - avoid global variable
  - data member private
  - decomposing large func

继承
- 多态-虚函数： 运行时多态，随类实例的变化而变化函数
- 纯虚函数：初始未定义，抽象类无法实例化
- protected：基类及继承类私有
- 虚函数表
- dynamic_cast

‌C++实现多态的三种形式是‌：
- ‌静态多态性（早绑定）‌：使用‌函数重载和运算符重载实现，编译器在编译时确定调用的函数或运算符。
- ‌动态多态性（晚绑定）‌：使用‌虚函数和‌指针/引用实现，运行时根据对象的实际类型确定调用的函数。
- ‌‌纯虚函数和‌抽象类‌：通过将虚函数声明为纯虚函数，将类定义为抽象类，实现接口的统一定义，子类必须实现纯虚函数才能实例化。
```c++
class Document
{
public:
 /* Constructor, destructor, etc. */
    virtual int* convertToPixelArray() const = 0;// not exists
    virtual string getDocumentName() const = 0;
private:
 /* Implementation details */
};
```

const
- variable objects: not modify
  - const int a=5;
- func: not modify instance variable
  - double getX() const;
- reference: pass-by-value
- pointer
  - const int*: can not modify pointee
  - int* const: can not modify pointer
  - const int* const: all const
- principle
  - not pass by value
  - function not change should const
  - variable never modified shouble const
```c++
SimpleClass::SimpleClass() : myInt(5), myString("C++!"), myVector(10) {}// const member assign
```
static
- variable: class global
- member function: ignore invisible this